/**
 * Templates — barrel export
 *
 * Page-level layout shells without real data.
 * Templates define structure; Pages fill them with content.
 *
 * Import from here: import { AuthTemplate, AppTemplate } from '@/components/templates';
 */

export { default as AuthTemplate } from './AuthTemplate';
export { default as AppTemplate } from './AppTemplate';
